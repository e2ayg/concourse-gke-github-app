# Pinned with conservative ranges; resolved at image build time.
# Verify latest stable releases before building for production.
PyJWT>=2.8,<3.0
cryptography>=42.0,<44.0
requests>=2.34.2,<3.0
google-cloud-secret-manager>=2.20,<3.0
kubernetes>=29.0,<32.0
